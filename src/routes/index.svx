# Button component

Here is an example:

```svelte example
<script>
    import Button from '$lib/Button.svelte'
    let label = "hello"
    label = `${label} world`
</script>

<Button>{label}</Button>

```
